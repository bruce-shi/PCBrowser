<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
     dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="server_url"
        android:title="@string/server_url"
        android:defaultValue="mobile_news"
        android:selectAllOnFocus="true"
        android:inputType="textCapWords"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />


    <EditTextPreference
        android:key="update_period"
        android:title="@string/update_period_title"
        android:defaultValue="60"
        android:digits="0123456789"
        android:inputType="number"
        android:singleLine="true"
        android:maxLines="1"
      />
    <SwitchPreference
        android:key="wifi_only"
        android:title="@string/only_wifi_title"
        android:defaultValue="false"
            />
    <SwitchPreference
            android:key="auto_push"
            android:title="@string/auto_push_title"
            android:defaultValue="true"
            />
    <SwitchPreference
            android:key="charge_only"
            android:title="@string/only_charge_title"
            android:defaultValue="false"
            />
</PreferenceScreen>
